{
  "name": "shortsmachine-api",
  "version": "1.0.0",
  "description": "Key-proxy backend for ShortsMachine — keeps API keys safe on the server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18.0.0" },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
